<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Password Reset</title>
    <link rel="icon" href="/images/logoacad.png">

    <style>
        body {
          background-color: #f8f8f8;
          font-family: Arial, sans-serif;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
        }
  
        h1 {
          color: #d00;
        }
  
        label {
          display: block;
          margin-bottom: 10px;
          font-size: 16px;
          color: #d00;
        }
  
        textarea[type="email"] {
          display: block;
          width: 100%;
          padding: 10px;
          border: 2px solid #d00;
          border-radius: 5px;
          font-size: 16px;
          margin-bottom: 20px;
        }
  
        button[type="submit"] {
          background-color: #d00;
          color: #fff;
          padding: 10px 20px;
          border: none;
          border-radius: 5px;
          font-size: 16px;
          cursor: pointer;
        }
  
        button[type="submit"]:hover {
          background-color: #c00;
        }
  
        p.error {
          color: #d00;
        }
  
        p.success {
          color: #0d0;
        }
        
        img {
          width: 200px;
          height: 180px;
          margin-bottom: 20px;
        }
      </style>
  </head>
  <body>
    <img src="/images/logoacad.png">
    <% if (typeof error !== 'undefined') { %>
        <p class="error"><%= error %></p>
      <% } else if (typeof success !== 'undefined') { %>
        <p class="success"><%= success %></p>
      <% } %>
      
    <form method="POST" action="/accountsettings/password_reset">
      <h1>Reset Your Password</h1>
      <label for="email">Email:</label>
      <textarea type="email" id="email" name="email" required></textarea>
      <button type="submit">Reset Password</button>
    </form>
  </body>
</html>
